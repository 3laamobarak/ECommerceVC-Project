namespace ECommerceModels.Enums
{
    public enum UserRole
    {
        Client,
        Admin,
        SuperAdmin
    }

    public enum OrderStatus
    {
        Pending,
        Approved,
        Denied,
        Shipped
    }
    public enum IsActive
    {
        Active,
        Inactive
    }
}